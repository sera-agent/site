title: localai
tagline: Affordable AI inference hardware for local businesses
description: Build and service custom inference servers under €10,000 — private, on-premise AI without cloud costs.

url: "https://seramono.mooo.com"
baseurl: ""

theme: minima

plugins:
  - jekyll-seo-tag

# Minima settings
minima:
  social_links:
    - { platform: email, user_url: "mailto:hello@localai.example" }

# Markdown
markdown: kramdown
kramdown:
  input: GFM

# Exclude
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - vendor
